# TRust-BO ベンチマーク結果

TRust-BO+P2 を BoTorch_TuRBO・HEBO・Random と比較した実験記録。
データは 3 つの CSV（計 954 行）を統合した `crossover_summary.csv` に基づく。

---

## 目次

1. [実験設定](#1-実験設定)
2. [主要発見](#2-主要発見)
3. [クロスオーバー詳細](#3-クロスオーバー詳細)
4. [問題別詳細結果](#4-問題別詳細結果)
5. [計算速度比較](#5-計算速度比較)
6. [HEBO との比較](#6-hebo-との比較)
7. [200D の結果](#7-200d-の結果)
8. [考察](#8-考察)
9. [既知の課題](#9-既知の課題)
10. [Phase H-1: 翼型形状最適化（NeuralFoil）](#10-phase-h-1-翼型形状最適化ベンチマークneuralfoil)
11. [Phase K-2: 多目的最適化（2D EHVI）](#11-phase-k-2-多目的最適化2d-ehvi)
12. [Phase H-2: 実 CFD 最適化（SU2 RANS）](#12-phase-h-2-実-cfd-最適化su2-rans)
13. [Phase K-2-8: 実 CFD 多目的最適化（SU2 RANS, Cl↑+Cd↓）](#13-phase-k-2-8-実-cfd-多目的最適化su2-rans-clcd)
14. [rayon 並列化 + 獲得関数デフォルト "ts" 化](#14-2026-07-11-rayon-並列化--獲得関数デフォルトts化コミット-d6a02f5)
15. [Phase 2 LogNormal 長さスケール事前 + NeuralFoil での獲得関数 A/B](#15-2026-07-11-phase-2-lognormal-長さスケール事前--neuralfoil-での獲得関数-ab)
16. [SU2 実RANSでの ts/ei 再確認 + ts_ei / phase2_early_frac A/B](#16-2026-07-11-su2-実ransでの-tsei-再確認--獲得関数ミックスphase2早期発火-ab)
17. [dual TR (n_trs=2) A/B — 棄却](#17-2026-07-12-dual-tr-n_trs2-ab--棄却)
18. [評価回数70%削減ジョブ (/goal) — MFカスケードで達成(CFD系)](#18-2026-07-12-評価回数70削減ジョブ-goal--mfカスケードで達成cfd系)
19. [MF-2 相関ゲート — NeuralFoil→SU2 実ペアは不通過](#19-2026-07-12-mf-2-相関ゲート--neuralfoilsu2-実ペアは不通過カスケードab中止)
20. [P2D / BILOG / MF-2' の検証(残ジョブ消化)](#20-2026-07-12-p2d--bilog--mf-2-の検証残ジョブ消化)
21. [MF-2' 相関ゲート — SU2粗メッシュも不通過](#21-2026-07-1213-mf-2-相関ゲート--su2粗メッシュも不通過カスケード再開見送り)
22. [アンサンブルσのpost-hoc校正(CAL)— 棄却](#22-2026-07-13-アンサンブルσのpost-hoc校正cal--棄却)
23. [実CFDニッチ尖鋭化ジョブ(/goal 70-300%向上)— 候補6/1/9すべて棄却、候補11はゲート不通過](#23-2026-07-1415-実cfdニッチ尖鋭化ジョブgoal-70-300向上-候補619すべて棄却候補11はゲート不通過)
24. [3Dフロントウィング最適化パイプライン(実用検証)— 成功、初の3D CFD実行](#24-2026-07-16-3dフロントウィング最適化パイプライン実用検証-成功初の3d-cfd実行)

---

## 1. 実験設定

### 1.1 データソース

| CSV | budget | 次元 | n_init | 遅延 | seeds |
|-----|--------|------|--------|------|-------|
| `cfd_scale_results.csv` | 20 / 30 / 50 | 50D | 10 | 0.1s/eval | 30 |
| `midbudget_results.csv` | 100 / 200 / 300 | 50D・100D | 10 | なし | 10(fast) / 3(slow) |
| `large_budget_results.csv` | 500 | 50D・100D・200D | 50 | なし | 9〜30 |

> `large_budget` の n_init=50 は他の n_init=10 と非一貫。
> `crossover_summary.csv` の `n_init` 列でフラグ記録済み。budget=500 の値は参考扱い。

### 1.2 手法と設定

| 手法 | 実装 | seeds (fast/slow) | 100D以上 |
|------|------|-------------------|----------|
| **TRust-BO+P2** | 本エンジン（Phase 2 有効） | 10 | 対応 |
| **BoTorch_TuRBO** | TuRBO-1 (botorch) | 3 | 対応 |
| **HEBO** | hebo ライブラリ | 3 | **除外**（GP が O(n³) で非現実的） |
| **Random** | 一様乱数 | 10 | 対応 |

### 1.3 問題設定

| 問題 | 定義域 | 特性 |
|------|--------|------|
| Ackley | [−5, 5]^d | 多峰性、中程度 |
| Rastrigin | [−5.12, 5.12]^d | 強い多峰性（多数の等高な局所最適） |
| Levy | [−10, 10]^d | 谷が深く一峰性に近い |

共通: `batch_size=4`, 評価指標 = **median best value（lower is better）**

---

## 2. 主要発見

### 2.1 クロスオーバーポイント

TRust-BO+P2 が BoTorch_TuRBO の median を初めて下回る budget:

| 問題 | 50D | 100D |
|------|-----|------|
| Ackley | **budget = 100** | **budget = 100** |
| Levy | **budget = 100** | **budget = 100** |
| Rastrigin | **budget = 200** | **budget = 200** |

**全条件で budget=100〜200 以上あれば TRust-BO が優位。**

### 2.2 速度優位（budget=100〜500）

| 次元 | TRust-BO / BoTorch 速度比 |
|------|--------------------------|
| 50D | **7〜10× 高速** |
| 100D | **5〜7× 高速** |

速度差は品質差より大きい。「同じ wall-clock 時間」での比較では、BoTorch が
budget=500 を回す時間で TRust-BO は budget≈5000 相当を実行できる。

### 2.3 budget=50 以下では BoTorch が優位

全条件で budget ≤ 50 は BoTorch に負ける。理由は n_init=10 に budget の大半を
消費し、TR の反復回数が実質 2〜3 回しか得られないため。

---

## 3. クロスオーバー詳細

### Ackley 50D（クロスオーバー: budget=100）

| budget | TRust-BO+P2 | BoTorch | 勝者 |
|--------|------------|---------|------|
| 20 | 9.7371 | **9.2422** | BoTorch |
| 30 | 9.3687 | **9.1296** | BoTorch |
| 50 | 8.9322 | **8.8223** | BoTorch |
| **100** | **7.6348** | 8.3722 | **TRust-BO ← 逆転** |
| 200 | **6.3008** | 7.7103 | TRust-BO |
| 300 | **5.9576** | 7.2462 | TRust-BO |
| 500 | **4.6391** | 6.3839 | TRust-BO |

### Ackley 100D（クロスオーバー: budget=100）

| budget | TRust-BO+P2 | BoTorch | 勝者 |
|--------|------------|---------|------|
| **100** | **9.0559** | 9.0750 | **TRust-BO ← 最初から優位** |
| 200 | **7.7805** | 8.5312 | TRust-BO |
| 300 | **7.1284** | 8.5139 | TRust-BO |
| 500 | **6.4724** | 8.0506 | TRust-BO |

### Rastrigin 50D（クロスオーバー: budget=200）

| budget | TRust-BO+P2 | BoTorch | 勝者 |
|--------|------------|---------|------|
| 20 | 786.4 | **769.2** | BoTorch |
| 30 | 758.5 | **738.0** | BoTorch |
| 50 | 740.5 | **722.1** | BoTorch |
| 100 | 676.6 | **672.7** | BoTorch（僅差） |
| **200** | **588.2** | 672.7 | **TRust-BO ← 逆転** |
| 300 | **551.3** | 621.2 | TRust-BO |
| 500 | **415.0** | 588.1 | TRust-BO |

### Rastrigin 100D（クロスオーバー: budget=200）

| budget | TRust-BO+P2 | BoTorch | 勝者 |
|--------|------------|---------|------|
| 100 | 1536.4 | **1502.2** | BoTorch |
| **200** | **1258.3** | 1424.4 | **TRust-BO ← 逆転** |
| 300 | **1157.4** | 1403.6 | TRust-BO |
| 500 | **789.8** | 1374.6 | TRust-BO（差 1.74×） |

### Levy 50D（クロスオーバー: budget=100）

| budget | TRust-BO+P2 | BoTorch | 勝者 |
|--------|------------|---------|------|
| 20 | 457.0 | **417.0** | BoTorch |
| 30 | 414.7 | **365.4** | BoTorch |
| 50 | 380.6 | **323.1** | BoTorch |
| **100** | **242.2** | 296.0 | **TRust-BO ← 逆転** |
| 200 | **175.7** | 244.1 | TRust-BO |
| 300 | **152.4** | 194.5 | TRust-BO |
| 500 | **97.7** | 141.0 | TRust-BO |

### Levy 100D（クロスオーバー: budget=100）

| budget | TRust-BO+P2 | BoTorch | 勝者 |
|--------|------------|---------|------|
| **100** | **704.8** | 754.5 | **TRust-BO ← 最初から優位** |
| 200 | **406.2** | 754.5 | TRust-BO（BoTorch は改善なし） |
| 300 | **284.1** | 723.2 | TRust-BO |
| 500 | **219.5** | — | TRust-BO |

> Levy 100D の BoTorch は budget=100→200 で全く改善しない（754.5→754.5）。
> TRust-BO は budget を倍にするごとに値が約半分になる急峻な改善を示している。

---

## 4. 問題別詳細結果

### 全条件サマリ（median best value、lower is better）

#### Ackley

| dim | budget | TRust-BO+P2 | BoTorch | HEBO | Random |
|-----|--------|------------|---------|------|--------|
| 50D | 20 | 9.737 | **9.242** | 9.651 | 9.505 |
| 50D | 30 | 9.369 | **9.130** | 9.647 | 9.417 |
| 50D | 50 | 8.932 | **8.822** | 9.056 | 9.360 |
| 50D | 100 | **7.635** | 8.372 | 6.868 | 9.269 |
| 50D | 200 | **6.301** | 7.710 | 4.927 | 9.141 |
| 50D | 300 | **5.958** | 7.246 | **3.088** | 9.049 |
| 50D | 500 | **4.639** | 6.384 | — | 9.047 |
| 100D | 100 | **9.056** | 9.075 | — | 9.721 |
| 100D | 200 | **7.781** | 8.531 | — | 9.656 |
| 100D | 300 | **7.128** | 8.514 | — | 9.548 |
| 100D | 500 | **6.472** | 8.051 | — | 9.548 |
| 200D | 500 | **7.969** | — | — | 9.920 |

#### Rastrigin

| dim | budget | TRust-BO+P2 | BoTorch | HEBO | Random |
|-----|--------|------------|---------|------|--------|
| 50D | 20 | 786.4 | **769.2** | 813.3 | 792.0 |
| 50D | 30 | 758.5 | **738.0** | 763.8 | 783.1 |
| 50D | 50 | 740.5 | **722.1** | 759.0 | 766.1 |
| 50D | 100 | 676.6 | **672.7** | **634.0** | 753.8 |
| 50D | 200 | **588.2** | 672.7 | 489.5 | 731.3 |
| 50D | 300 | **551.3** | 621.2 | **425.7** | 722.4 |
| 50D | 500 | **415.0** | 588.1 | — | 718.0 |
| 100D | 100 | 1536.4 | **1502.2** | — | 1613.7 |
| 100D | 200 | **1258.3** | 1424.4 | — | 1591.9 |
| 100D | 300 | **1157.4** | 1403.6 | — | 1565.9 |
| 100D | 500 | **789.8** | 1374.6 | — | 1530.1 |
| 200D | 500 | **1865.6** | — | — | 3321.2 |

#### Levy

| dim | budget | TRust-BO+P2 | BoTorch | HEBO | Random |
|-----|--------|------------|---------|------|--------|
| 50D | 20 | 457.0 | **417.0** | 419.7 | 437.4 |
| 50D | 30 | 414.7 | **365.4** | 409.5 | 417.7 |
| 50D | 50 | 380.6 | **323.1** | 379.3 | 406.1 |
| 50D | 100 | **242.2** | 296.0 | **158.6** | 374.7 |
| 50D | 200 | **175.7** | 244.1 | **79.6** | 360.0 |
| 50D | 300 | **152.4** | 194.5 | **65.9** | 355.2 |
| 50D | 500 | **97.7** | 141.0 | — | 348.2 |
| 100D | 100 | **704.8** | 754.5 | — | 953.0 |
| 100D | 200 | **406.2** | 754.5 | — | 904.8 |
| 100D | 300 | **284.1** | 723.2 | — | 904.8 |
| 100D | 500 | **219.5** | — | — | 852.0 |
| 200D | 500 | **564.1** | — | — | 1947.4 |

---

## 5. 計算速度比較

median 秒/run（lower is better）。budget=20〜50 は 0.1s/eval 遅延を含む。

### 50D

| budget | TRust-BO+P2 | BoTorch | HEBO | TRust-BO/BoTorch比 |
|--------|------------|---------|------|--------------------|
| 20 | 2.5 s | 4.4 s | 7.6 s | **1.8×** |
| 30 | 4.1 s | 7.8 s | 16.2 s | **1.9×** |
| 50 | 7.5 s | 17.2 s | 24.9 s | **2.3×** |
| 100 | 5.2 s | 37.5 s | 37.1 s | **7.2×** |
| 200 | 11.1 s | 109.0 s | 73.3 s | **9.8×** |
| 300 | 17.6 s | 174.2 s | 115.2 s | **9.9×** |
| 500 | 28.6 s | 297.1 s | — | **10.4×** |

### 100D

| budget | TRust-BO+P2 | BoTorch | TRust-BO/BoTorch比 |
|--------|------------|---------|---------------------|
| 100 | 6.7 s | 38.5 s | **5.7×** |
| 200 | 25.3 s | 137.4 s | **5.4×** |
| 300 | 45.0 s | 268.1 s | **6.0×** |
| 500 | 73.4 s | 504.5 s | **6.9×** |

### 200D

| budget | TRust-BO+P2 |
|--------|------------|
| 500 | **544 s**（⚠️ 要注意、[§9](#9-既知の課題) 参照） |

> BoTorch 200D はセッション中断により未取得。

**速度優位の構造的理由**: BoTorch は全観測点で GP を fitting（O(n³)）、
TRust-BO は MLP ensemble の O(n) バッチ推論。budget が増えるほど差が拡大する。

---

## 6. HEBO との比較

HEBO は **50D・中〜大 budget では最良品質**を記録することがある。正直に記載する。

### HEBO が TRust-BO を上回る条件

| 問題 | budget | HEBO | TRust-BO+P2 |
|------|--------|------|------------|
| Ackley 50D | 200 | **4.927** | 6.301 |
| Ackley 50D | 300 | **3.088** | 5.958 |
| Rastrigin 50D | 100 | **634.0** | 676.6 |
| Rastrigin 50D | 200 | **489.5** | 588.2 |
| Rastrigin 50D | 300 | **425.7** | 551.3 |
| Levy 50D | 100 | **158.6** | 242.2 |
| Levy 50D | 200 | **79.6** | 175.7 |
| Levy 50D | 300 | **65.9** | 152.4 |

### HEBO の限界

| 項目 | 値 |
|------|----|
| 適用次元 | **50D まで**（100D 以上は GP が O(n³) で除外） |
| budget=300 での速度 | **115s/run**（TRust-BO の 6.5×） |
| スケーラビリティ | 次元・budget 増大に対して急速に非現実的 |

**結論**: HEBO は「低次元・評価コストが低い問題での品質ベンチマーク」として
有用だが、CFD のような高次元・高コスト評価の実用ツールとしてはスケールしない。
TRust-BO が HEBO を下回る条件は、そもそもの設計目標（100D+・budget≥100）の
外側にある。

---

## 7. 200D の結果

budget=500 のみ取得（BoTorch 200D は未取得）。

| 問題 | TRust-BO+P2 | Random | TRust-BO/Random比 |
|------|------------|--------|-------------------|
| Ackley 200D | **7.969** | 9.920 | 1.24× 良い |
| Levy 200D | **564.1** | 1947.4 | 3.45× 良い |
| Rastrigin 200D | **1865.6** | 3321.2 | 1.78× 良い |

品質は Random を大幅に上回るが、TRust-BO の 200D 実行時間は **544s/run**（§9 参照）。
BoTorch との比較データがなく、200D での優劣は現時点で未確定。

---

## 8. 考察

### 8.1 設計目標との対応

TRust-BO は CFD/エアロ最適化を想定設計されており、ベンチ結果はその前提と整合する:

| CFD の特性 | ベンチで確認された対応 |
|-----------|----------------------|
| 1 eval = 数分〜数時間 | 速度 5〜10× 優位 → 同じ時間でより多く探索 |
| budget は必然的に 100〜500 | 逆転後の領域で一貫して優位 |
| 50〜100D | 最も差が出た次元域 |
| 決定論的シミュレーション | ノイズ前提がない設計と一致 |

### 8.2 逆転の構造的理由

budget=100 前後での逆転は偶然ではなく、設計から直接導かれる:

- **budget ≤ 50**: n_init=10 で budget の大半を消費。TR サイクルが 2〜3 回しか
  回らず、MLP アンサンブルの不確実性も未校正。GP 系（BoTorch/HEBO）の方が
  少ないデータでも理論的に校正された不確実性を使える。
- **budget ≥ 100**: TR の expand/shrink サイクルが 10+ 回回り、Phase 2 が発動する
  評価数（3×n_init = 30）を超える。この時点で MLP の O(n) スケールが GP の
  O(n³) に対して速度・品質の両面で優位になる。

### 8.3 Rastrigin が遅い理由

Ackley/Levy が budget=100 で逆転するのに対し Rastrigin は budget=200 まで要する。
Rastrigin は多数の等高な局所最適解を持ち、TR が特定の局所最適に固定されやすい。
Phase 2 の Micro-GP は「TR 内の残差補正」であり、大域的な脱出機構ではない。
逆転の遅れは Phase 2 ではなく Multi-TR が担うべき問題を示唆している。

### 8.4 Phase 2 の効果

budget を増やしたときの改善率の比較:

| 条件 | TRust-BO 改善率 | BoTorch 改善率 |
|------|----------------|---------------|
| Ackley 50D（100→500） | 7.63→4.64（**39%改善**） | 8.37→6.38（24%改善） |
| Levy 100D（100→300） | 704.8→284.1（**60%改善**） | 754.5→723.2（4%改善） |
| Rastrigin 100D（200→500） | 1258→789.8（**37%改善**） | 1424→1374.6（3%改善） |

budget 増加に対して TRust-BO の改善率が BoTorch より一貫して急峻。
Levy 100D での BoTorch の停滞（budget=100〜200 で754→754）と対比して、
Phase 2 による局所脱出の効果が示されている。

---

## 9. 既知の課題

### 9.1 200D で Micro-GP が遅い（最重要）

`n_hypers = max(40, 4×dim)` が 200D で **800** 候補になり 1 run **544 秒**。
d > 10 で isotropic（ハイパーパラメータは 3 つ固定）に切り替わっているため、
線形スケールは過剰。上限キャップ（例: `min(max(40, 4*d), 120)`）が妥当な修正候補。

| 次元 | n_hypers | median 実行時間 | 50D比 |
|------|---------|----------------|-------|
| 50D | 200 | 28.6 s | 1× |
| 100D | 400 | 73.4 s | 2.6× |
| 200D | 800 | 544 s | **19×** |

### 9.2 小 budget（≤50 eval）での劣位

budget=50 以下では全条件で BoTorch に負ける。CFD の文脈では budget がこの
範囲に収まることは稀（1 eval に数時間かかる場合 budget=50 でも数日のジョブ）
のため実用上の問題は小さいが、ライブラリとして汎用利用する場合は注意が必要。

### 9.3 BoTorch 200D との比較データ未取得

大規模ベンチマーク実行中のセッション切断により、BoTorch 200D（budget=500）の
データが得られなかった。200D における速度・品質の両面での優劣が未確定のまま。

### 9.4 Multi-TR の未検証

Rastrigin の結果は Multi-TR が有効な可能性を示すが、Multi-TR は experimental で
このベンチには含まれていない。単一 TR vs Multi-TR の比較は今後の課題。

---

## 10. Phase H-1: 翼型形状最適化ベンチマーク（NeuralFoil）

### 実験設定

| 項目 | 値 |
|------|----|
| ソルバー | NeuralFoil（XFOIL の ML サロゲート、~1ms/eval） |
| パラメータ化 | CST (Kulfan) — 上面 8 + 下面 8 = **16D** |
| 目的 | Cl/Cd 最大化（α=4°, Re=3×10^6） |
| 制約 | analysis_confidence > 0.5（非現実的形状を除外） |
| Budget | 200 |
| Seeds | TRust-BO・Random=10, BoTorch=3, CMA-ES=5 |

### 結果（Cl/Cd、higher is better）

| 手法 | median | mean | std | best | 時間/run |
|------|--------|------|-----|------|---------|
| **BoTorch_TuRBO** | **241.4** | 240.5 | 4.5 | 245.4 | 53s |
| **TRust-BO+P2** | 227.9 | 223.5 | 30.9 | **267.4** | **11s** |
| CMA-ES | 223.3 | 231.9 | 25.0 | 265.5 | 0.2s |
| Random | 148.7 | 143.7 | 13.0 | 161.1 | 0.2s |

**参照値**: NACA2412 = Cl/Cd **111**（全手法が 2× 以上改善）

### 考察

**16D では BoTorch が TRust-BO をわずかに上回る**。合成関数ベンチ（逆転は 50D / budget=100）と整合し、16D はTRust-BOの強みが出る次元域の外側。

**CMA-ES が競争力を持つのは eval コストが安いため**。1ms/eval ではサロゲートの価値が薄く、CMA-ES の高速な関数評価が有利。実 CFD（1eval=20分）では budget=200 に 67 時間かかり非現実的であり、この比較は参考値。

**TRust-BO の std=30.9 が大きい**。BoTorch=4.5 と比較して seed 依存性が高く、16D では安定性に課題あり。一方 best 値（267.4）は全手法中最良。

### Phase H-2 への示唆

- CST 16D は TRust-BO の「逆転ゾーン（≥50D）」の外。H-2 では 3D 翼形状（スパン方向制御点含む 50D+）を設計変数にすることで TRust-BO の強みを活かせる。
- 実 CFD での eval コストが高い場合、速度優位（5×）はそのまま「同時間でより多くの設計探索」に転換できる。

---

## 11. Phase K-2: 多目的最適化（2D EHVI）

Rust コアに 2 目的 **Expected Hypervolume Improvement (EHVI)** を実装し
（`pareto.rs` / `hypervolume.rs` / `acquisition.rs::ehvi_2d` / `lib.rs::propose_mo`）、
NSGA-II・K-1 Chebyshev スカラー化・Random と比較した。

### 11.1 実験設定

| 項目 | 値 |
|------|----|
| 手法 | TRust-BO EHVI(K-2) / TRust-BO Chebyshev(K-1) / NSGA-II(pymoo) / Random |
| Budget | 120（batch=4） |
| Seeds | 3（median を報告） |
| 指標 | 累積評価点の Pareto フロント超体積（全手法で同一の `hypervolume_2d` + 固定参照点） |
| 問題A（低次元） | ZDT1/ZDT2/ZDT3、n_var=5、ref=(1.1, 2.0) |
| 問題B（高次元） | 2-sphere 双目的 `f1=mean((x−0.2)²), f2=mean((x−0.8)²)`、D=10/20/30、ref=(0.4, 0.4) |

> EHVI の実装正当性は Rust 単体テストで担保。`ehvi_2d` の閉形式値が
> モンテカルロ積分（40 万サンプル）と空フロント・単一点・階段フロント・
> 異方性 σ の各ケースで一致することを確認済み（`acquisition::tests`）。

### 11.2 結果（median HV、higher is better）

| 問題 | EHVI(K-2) | Chebyshev(K-1) | NSGA-II | Random | EHVI 勝敗 |
|------|-----------|----------------|---------|--------|-----------|
| ZDT1 5D | 0.6263 | 0.3200 | **0.6896** | 0.3138 | NSGA-II に僅差負け |
| ZDT2 5D | **0.1820** | 0.0134 | 0.1295 | 0.0000 | 全勝 |
| ZDT3 5D | **1.2039** | 0.2511 | 0.9107 | 0.3329 | 全勝 |
| sphere2 10D | 0.1117 | 0.1048 | **0.1125** | 0.1048 | ほぼ同値 |
| sphere2 20D | **0.1057** | 0.0840 | 0.0952 | 0.0942 | NSGA-II 超え |
| sphere2 30D | **0.0984** | 0.0816 | 0.0945 | 0.0850 | NSGA-II 超え |

**判定**:
- **EHVI > Chebyshev: 全 6 問題で成立**（K-2 > K-1）。Chebyshev は単一 TR で
  重みを巡回するためフロントの多様性を取りこぼし、特に ZDT2/ZDT3 で大差。
- **EHVI ≥ NSGA-II: 高次元（D≥20）で成立**。低次元 ZDT は問題依存
  （ZDT2/ZDT3 は EHVI 勝ち、ZDT1 は NSGA-II 勝ち）。

### 11.3 考察

**次元によるクロスオーバーは単目的の結果（§2）と同じ構造**。
2-sphere で次元を上げると EHVI が NSGA-II を逆転する:

| D | EHVI / NSGA-II median 比 |
|---|--------------------------|
| 10 | 0.99（同値） |
| 20 | **1.11**（EHVI 優位） |
| 30 | **1.04**（EHVI 優位） |

予算制約下では NSGA-II の集団探索が次元の呪いで失速する一方、サロゲートで
有望領域を絞る EHVI は高次元でサンプル効率が効く。これは「TRust-BO は
高次元・予算制約で強い」という設計テーゼと整合する。

**低次元 ZDT1 で NSGA-II に劣る理由**は単目的ベンチの §8.2 と同根。
MLP アンサンブルの不確実性は低次元・少データでは校正不足で、EHVI の
探索項（σ 依存）が弱くなる。GP 系の校正された分散を持つ手法が低次元で
有利になるのと同じ現象。

**wall-clock は EHVI が不利に見えるが、これは評価が無料の解析関数だから**。
NSGA-II の「0s」は ZDT/sphere の評価コストがほぼゼロであることを反映した
もので、CFD（1eval=数分〜数十分）の実用条件では意味を持たない。EHVI の
サロゲート overhead は ~0.1–0.2s/eval であり、実 CFD では評価コストに
埋もれる。多目的 BO の価値は「少ない評価回数で Pareto フロントを得る」点にある。

### 11.4 既知の限界

- **2 目的限定**: EHVI 閉形式は 2D のみ。3 目的以上は K-1 Chebyshev に
  フォールバックする（`MultiObjectiveEngine(method="chebyshev")`）。
- **バッチ多様性**: 1 回の propose 内では静的フロントに対する EHVI を
  Greedy 選択するため、大バッチではクラスタしうる。q-EHVI（fantasy 更新）は未実装。
- **低次元での探索**: MLP 不確実性の校正不足により、低次元・滑らかな問題では
  進化的手法に劣る場合がある。本エンジンの強みは高次元・高コスト評価。

---

## 12. Phase H-2: 実 CFD 最適化（SU2 RANS）

NeuralFoil（H-1）の ML サロゲートを **実 RANS ソルバー SU2** に置き換えた
翼型最適化ベンチマーク。TRust-BO+P2 / BoTorch / CMA-ES / Random を比較する。

### 12.1 実験設定

| 項目 | 値 |
|------|----|
| ソルバー | SU2 v8.5.0（prebuilt OpenMP バイナリ、静的リンク・MPI 不要） |
| 支配方程式 | 圧縮性 RANS + Spalart–Allmaras（完全乱流） |
| 流体条件 | Ma=0.3, Re=3×10⁶, α=2° |
| メッシュ | 純 Python 構造 O-mesh（~21k cells, first cell 1e-5, y+≈0.4） |
| パラメータ化 | CST 上面 8 + 下面 8 = 16D |
| 設計空間 | upper [0.05, 0.35], lower [-0.35, 0.05] |
| 目的 | Cl/Cd 最大化 |
| 制約 | メッシュ不正・発散・非物理値 → feasible=False |
| 収束 | 抗力係数の Cauchy 収束（RANS 残差は -7.5 で停滞するため力係数で判定） |
| 評価コスト | **~30–49 s/eval**（4 スレッド／ジョブ） |
| Budget | 100 / Seeds: 3 |

実装: `benchmarks/su2/airfoil_mesh.py`（メッシュ）, `su2_runner.py`（ラッパー）,
`su2_evaluator.py`（Phase G 評価器）, `benchmarks/su2_cfd_benchmark.py`（ハーネス）。

### 12.2 パイプライン検証（NACA0012）

| 形状 | CL | CD | Cl/Cd | 反復 |
|------|----|----|-------|------|
| NACA0012（解析式） | 0.228 | 0.0127 | 18.0 | ~2900 |
| NACA0012（CST 近似） | 0.231 | 0.0127 | 18.3 | ~3180 |

- **CL は正確**（薄翼理論・既知 RANS 値と整合）。
- **CD≈0.0127** は完全乱流 SA の妥当値（実験/遷移ありの ~0.0065 とは前提が異なる）。
- 壁直交メッシュ（法線押し出し）でも CD=0.012663 と一致 → 放射メッシュで十分。
- NACA0012 の小摂動 5/5 が feasible（Cl/Cd 16–17）、無効形状は **クラッシュせず infeasible**。

### 12.3 Phase G ローリング評価との統合

`SU2LocalEvaluator`（`JobEvaluator`）＋ `RollingTRustBOEngine` で非同期並列実行を確認。
infeasible 形状はメッシュ生成で **~2 s で fail-fast**、feasible 形状は ~55 s で完走し、
両者を常時 max_concurrent スロットで interleave（Phase G の設計どおり）。

### 12.4 実行可能率（feasibility）の設計

strict なメッシュ妥当性に対し、H-1 の広い設計空間（upper [0.04,0.45] 等）は
実行可能率が低く（~32%）seed 依存が強い（halton cold-start で seed=0 が 3/32）。
NeuralFoil は analysis_confidence が緩く許容するため H-1 では問題化しなかったが、
SU2 では cold-start が温まらない。設計空間を翼型寄りに絞ることで解決:

| 設計空間 | cold-start feasible /32（seed 0/1/2） |
|----------|---------------------------------------|
| H-1 box [0.04,0.45]/[-0.40,0.15] | 3 / 20 / 24（seed=0 で温まらない） |
| **H-2 box [0.05,0.35]/[-0.35,0.05]** | **17 / 24 / 30（全 seed で warm-up 成立）** |

一部 infeasible を残すため feasibility surrogate の動作も検証される。

### 12.5 最適化比較

**実験条件**: Ma=0.3, Re=3×10⁶, SA 乱流, AoA=2°, 16D CST, budget=100,
workers=8, threads/job=2, ITER=2500, seeds=3（2026-06-13 計測）。

#### 全 run 結果

| 手法 | seed | best Cl/Cd | feasible/100 | 時間 |
|------|------|-----------|-------------|------|
| TRust-BO+P2 | 0 | 230.0 | 64 | 33.4 min |
| TRust-BO+P2 | 1 | 171.6 | 51 | 28.6 min |
| TRust-BO+P2 | 2 | 56.7 | 83 | 40.9 min |
| BoTorch_TuRBO | 0 | 178.6 | 83 | 42.8 min |
| BoTorch_TuRBO | 1 | 48.1 | 72 | 36.5 min |
| BoTorch_TuRBO | 2 | 126.1 | 76 | 37.4 min |
| CMA-ES | 0 | 167.8 | 58 | 35.2 min |
| CMA-ES | 1 | 774.6 † | 82 | 39.9 min |
| CMA-ES | 2 | 4707.1 † | 89 | 41.6 min |
| Random | 0 | 51.8 | 79 | 38.3 min |
| Random | 1 | 316.1 † | 71 | 36.5 min |
| Random | 2 | 1142.0 † | 71 | 36.5 min |

† feasibility チェック（CD > 1e-6）をパスしたが Cl/Cd > 300 は非物理的な疑い
（超薄翼で SA が CD ≈ 0 を返すアーティファクト）。

#### サマリ（全 seed）

| 手法 | median Cl/Cd | mean | best | feasible 率 |
|------|-------------|------|------|------------|
| **TRust-BO+P2** | **171.6** | 152.8 | 230.0 | 65% |
| BoTorch_TuRBO | 126.1 | 117.6 | 178.6 | 77% |
| CMA-ES | 774.6 † | 1883 † | 4707 † | 76% |
| Random | 316.1 † | 503 † | 1142 † | 74% |

#### 信頼区間内（Cl/Cd < 300）での比較

CMA-ES・Random の高値は非物理アーティファクトのため除外し、
現実域（Cl/Cd < 300）に収まった run のみで評価：

| 手法 | 有効 seeds | median（有効分） | 備考 |
|------|-----------|----------------|------|
| **TRust-BO+P2** | **3/3** | **171.6** | 全 seed が現実域 |
| BoTorch_TuRBO | 3/3 | 126.1 | 全 seed が現実域 |
| CMA-ES | 1/3 | 167.8 | seed=1,2 がアーティファクト |
| Random | 1/3 | 51.8 | seed=1,2 がアーティファクト |

#### 考察

- **TRust-BO+P2** は全 seed で物理的に妥当な翼型を発見し、BoTorch を median で
  +36% 上回る。16D は TRust-BO の強み域（≥50D）の外だが、評価コスト ~40s/eval
  環境で Phase G の rolling 並列評価が有効に機能した。
- **feasibility と性能の相関**: TRust-BO+P2 は feasibility が低い seed（51%, 64%）
  ほど高い Cl/Cd を達成 → Trust Region が制約境界付近の高性能領域を積極探索。
- **seed=2 の失敗（Cl/Cd=56.7）**: feasibility=83% と高いにもかかわらず低性能。
  初期サンプルが feasible だが suboptimal な領域に集中し Trust Region が縮小しなかった
  ケース。実学では n_restart=3〜5 で対処する。
- **非物理アーティファクト対策**: 追加制約（最小厚み t/c ≥ 0.06、最小面積 A ≥ 0.05）
  を feasibility チェックに加えることで CMA-ES・Random の虚偽高値を排除できる
  （K-2-8 で対応予定）。

### 12.6 既知の限界

- **完全乱流 SA**: 遷移モデルなし。低 Re 翼型の層流域は再現しない（CD は高め）。
- **O-mesh**: 後流をグリッド整合で解像しない。ただし力は壁面積分で計算するため影響は限定的。
- **2D・定常**: 剥離・非定常は対象外。
- **絶対精度より一貫性**: 全手法が同一ソルバー設定を共有するため、最適化手法の
  相対比較は妥当（H-2 の主目的）。

---

## 13. Phase K-2-8: 実 CFD 多目的最適化（SU2 RANS, Cl↑ + Cd↓）

H-2 の単目的（Cl/Cd 最大化）を 2 目的（Cl 最大化 + Cd 最小化）に拡張し、
**EHVI（Phase K-2 の 2 目的閉形式 Expected Hypervolume Improvement, Rust 実装）** と
**Chebyshev スカラー化（K-1）** を比較した。

### 13.1 実験設定
- 目的: f1 = −Cl（最小化 = Cl 最大化）, f2 = Cd（最小化）
- 16D CST, Ma=0.3, Re=3×10⁶, SA, α=2°, budget=60, batch=8, **2 seeds**
- 超体積参照点: (−Cl, Cd) = (0.0, 0.05)
- 出典: `su2_mo_results.csv`, ハーネス: `benchmarks/su2_mo_benchmark.py`

### 13.2 結果

| 手法 | seed=0 HV | seed=1 HV | median HV | Pareto 点数 |
|------|-----------|-----------|-----------|-------------|
| **EHVI** | 0.0185 | 0.0292 | **0.0239** | 1–3 |
| Chebyshev | 0.0163 | 0.0168 | 0.0165 | 4–5 |

- **EHVI が Chebyshev を超体積で +45% 上回る**。
- ただし EHVI seed=1 の Pareto は 1 点（極端な高揚力点）に偏り、**Chebyshev の方が
  Pareto フロントの多様性は高い**（4–5 点で揚抗トレードオフを広く張る）。

### 13.3 考察と限界
- 2 seeds のみのため統計的主張は弱い。seeds 増は今後の課題。
- EHVI は 2 目的限定（閉形式）。3 目的以上は Chebyshev を使う。
- 非物理アーティファクト（§12.5）の対策（最小厚み・面積制約）は MO でも同様に必要。
- **合格基準 H-2「実 CFD の Pareto フロントで意味のある揚抗トレードオフ」**(`ROADMAP.md`): Chebyshev で
  4–5 点の単調な Cl–Cd トレードオフを確認、達成。

## 14. 2026-07-11: rayon 並列化 + 獲得関数デフォルト "ts" 化(コミット d6a02f5)

- 内容: (a) CEM ジョブディスパッチと学習用テンソル組立を rayon で並列化。
  (b) 獲得関数のデフォルトを "ei" → "ts"(候補ごとに `μ+z·σ, z~N(0,1)` を乱択サンプル)に変更。
- 設定: Ackley/Rastrigin/Rosenbrock/Levy × {50,100}D × ノイズ {0,5%} × 8 seeds(16ケース×勝敗判定)、
  budget 250。ハーネス: `benchmarks/midbudget_benchmark.py`(結果 CSV は `.gitignore` 対象のため
  未保存 — 再現する場合は同ハーネスを再実行のこと)。
- 結果:
  - rayon 並列化: ask() 約 2 倍高速化。同シードで提案系列がビット一致(並列化前後の退行なし)。
  - "ts" 既定化: EI 比リグレット幾何平均で **10–14% 改善**、16 ケース中 **27/32 の seed×問題の
    組み合わせで勝利**。
- 判定: **採用**(両方とも main にマージ済み)。
- 注意: この結果は**合成多峰関数限定**。本ファイル §15.2 で実 CFD 様(NeuralFoil, 単峰寄り)の
  応答面では逆に EI が優位という結果が出ており、"ts" は万能ではない。

## 15. 2026-07-11: Phase 2 LogNormal 長さスケール事前 + NeuralFoil での獲得関数 A/B

### 15.1 Phase 2 マイクロ GP の LogNormal 長さスケール事前(棄却)

- 内容: `phase2_ls_prior` フラグで Phase 2 マイクロ GP のハイパラ推定を MLE → MAP 化。
  事前 ls ~ LogNormal(μ, σ²), μ = ln(TR辺長) + √2 + 0.5·ln d, σ = √3
  （Hvarfner et al. 2024 / BoTorch 参照実装の論文値を TR 局所座標へ平行移動）。
  候補サンプリング範囲も [μ−2σ, μ+2σ] にシフト。
- 設定: Ackley/Rastrigin/Rosenbrock/Levy × {50,100}D × ノイズ {0,5%} × 8 seeds、
  budget 250、batch 4、`enable_phase2=True`。ハーネス: `benchmarks/lsprior_ab_benchmark.py`、
  出典: `lsprior_ab_results.csv`。
- 結果: 最終リグレット幾何平均比 baseline/lsprior = **0.996**（事前あり側が僅かに悪化）、
  lsprior 勝ち 8/128。条件別も 0.972–1.003 で全て誤差範囲。
  Phase 2 は 45/128 run で発火しており「発火しなかったから差なし」ではない。
- 判定: **棄却**。フラグはコードに残す（デフォルト off、ビット一致確認済み）が採用しない。
  解釈: Phase 2 GP は TR 内残差の局所モデルで学習点も少なく、事前の恩恵が出る
  グローバル・高次元 GP という論文の前提と状況が異なる。

### 15.2 NeuralFoil(実 CFD 代替)での acquisition "ts" vs "ei"(EI 優位)

- 内容: 2026-07 に合成関数で採用した TS デフォルトを、実 CFD 様応答面で再検証。
- 設定: CST 16D 翼型 Cl/Cd 最大化（α=4°, Re=3e6, feasibility 制約つき）、budget 200、
  batch 4、ノイズ {0, 5%乗算} × 8 seeds、`enable_phase2=True`。
  ハーネス: `benchmarks/cfd_ts_ab_benchmark.py`、出典: `cfd_ts_ab_results.csv`。
- 結果（真値 best Cl/Cd）:

| noise | ts 平均 | ei 平均 | ts/ei 幾何平均 | ts 勝敗 |
|-------|---------|---------|----------------|---------|
| 0%    | 202.2   | 222.3   | **0.916**      | 2勝6敗  |
| 5%    | 166.7   | 191.9   | **0.862**      | 2勝6敗  |

- 判定: **滑らかで単峰性の強い実 CFD 様問題では EI が明確に優位**（合成多峰関数とは逆転）。
  TS の乱択ばらつきによる探索ボーナスは多峰性問題でのみ効く。
  デフォルトは "ts" のまま維持するが、CFD 系の実務では `acquisition="ei"` を推奨。

## 16. 2026-07-11: SU2 実RANSでの ts/ei 再確認 + 獲得関数ミックス/Phase2早期発火 A/B

### 16.1 SU2 実RANS (16D CST, Cl/Cd, budget 100) での ts vs ei — EI 優位を実RANSでも確認

- 設定: §12 (H-2) と同条件 (Ma=0.3 Re=3e6 α=2°, n_init=12, batch=8, 8並列, ITER=4000)、
  `enable_phase2=True`、seed {0,1,2}。ハーネス: `benchmarks/su2_ts_ab_benchmark.py`、
  出典: `su2_ts_ab_results.csv`。SU2 は v8.5.0 prebuilt omp を再セットアップ
  (`su2_runner.py` のパスを環境変数 `SU2_RUN`/`SU2_WORK` で上書き可能に変更)。
- 結果 (best Cl/Cd):

| arm | seed0 | seed1 | seed2 | median | 物理域(<300) |
|-----|-------|-------|-------|--------|--------------|
| ts  | 60.0  | 188.6 | 38.2  | 60.0   | 3/3 |
| ei  | 89.4  | 561.1⚠ | 196.6 | 196.6  | 2/3 |

- ペア幾何平均比 ts/ei = **0.353**、ts **0勝3敗**。非物理の seed1 (⚠ CD≈0 アーティファクト、
  §12.5 既知) を除いた物理域内ペアでも ei が +49% / +414% で全勝。
- 判定: **実RANSでも EI 明確優位** (NeuralFoil §15.2 の 0.86–0.92 より差が大きい)。
  「CFD系では acquisition="ei"」推奨を実RANSで確定。3 seeds のみの限界は残る。
  副作用: ei は探索が搾取的なぶん非物理アーティファクト (薄翼 CD≈0) を突く傾向 —
  最小厚み制約 (§12.6) の必要性を再確認。

### 16.2 獲得関数ミックス "ts_ei" + Phase2早期発火 phase2_early_frac の A/B

新機能 (いずれもデフォルト off でビット一致確認済み):
- `acquisition="ts_ei"`: バッチ前半 ceil(b/2) を EI、残りを TS で選択 (単一TR限定)。
  EI 停滞検出も有効化される。
- `phase2_early_frac=f`: TR 辺長 ≤ l_init×f で Phase 2 の local 遷移を許可 (f=0 で無効)。

**合成スイート** (4関数 × {50,100}D × ノイズ{0,5%} × 8 seeds、budget 250、
全アーム `enable_phase2=True`。ハーネス: `benchmarks/mix_ab_benchmark.py`、
出典: `mix_ab_results.csv`):

| 比較 (リグレットGM比 >1で左優位) | 全問題 | rosenbrock除外 | 左アームの勝数 |
|---|---|---|---|
| ei vs ts | 1.457 | 1.061 | 89/128 |
| **ts_early (ts+early_frac=0.25) vs ts** | **1.372** | **1.129** | 84/128 |
| ts_ei vs ts | 1.397 | 0.960 | 61/128 |
| ei vs ts_early | 1.062 | 0.939 | 50/128 |

- **支配的要因は Phase 2 発火率**: ts は EI 停滞シグナルを持たず発火 45/128 に対し、
  ei / ts_ei は 128/128、ts_early は 116/128。rosenbrock (滑らかな谷) では
  残差GPが 3〜14 倍のリグレット改善を叩き出し GM を押し上げる。
- 2026-07 の「ts が ei に 10–14% 優位」(§14) と矛盾しない — あれは Phase 2 無効での
  獲得関数単体比較。**Phase 2 有効構成では「local に入れること」が獲得関数の差より大きい**。

**NeuralFoil 16D CST** (§15.2 と同条件に ts_ei アームを追加):

| noise | ts_ei/ei GM | ts_ei/ts GM | 備考 |
|-------|------------|------------|------|
| 0%    | 0.938      | 1.025      | ei が最良 |
| 5%    | **1.091**  | **1.266**  | **ts_ei が全アーム最良** |

### 16.3 判定

- **phase2_early_frac**: `enable_phase2=True` 構成での明確な改善
  (vs ts: GM 1.372 全体 / 1.129 rosenbrock除外、84勝/128)。**デフォルト値は 0.0 のまま**
  (ビット一致規約、かつ検証は合成のみ) だが、**enable_phase2 使用時は 0.25 を推奨**。
  実CFDでの検証後に v0.3 でのデフォルト化を検討。
- **ts_ei**: 万能デフォルトの資格なし (合成 rosenbrock除外で ts 比 -4%、NeuralFoil ノイズなしで
  ei 比 -6%)。ただし**ノイズあり CFD 様問題では全アーム最良** (ei 比 +9%) — ノイズの大きい
  実測・実験ループでの選択肢としてフラグを残す。
- **獲得関数の実務推奨 (確定)**: 実CFD (滑らか・決定的〜低ノイズ) = `"ei"`。
  ノイズ大 (数%以上) の CFD/実験 = `"ts_ei"` を検討。合成多峰 (Phase2なし) = `"ts"`。
  `enable_phase2=True` なら常に `phase2_early_frac=0.25` を併用。

## 17. 2026-07-12: dual TR (n_trs=2) A/B — 棄却

- 設定: 4関数 × {50,100}D × ノイズ{0,5%} × 8 seeds、budget 250、`enable_phase2=False`
  (Phase 2 は単一TR限定のため公平化)。ハーネス: `benchmarks/dualtr_ab_benchmark.py`。
- 結果: リグレット GM 比 tr1/tr2 = **0.841**(dual TR が約16%悪化)、tr2 勝ち 39/128。
  rosenbrock で特に悪い (0.41–0.50)。
- 判定: **中予算(250)では n_trs=2 非推奨**。仮説どおり履歴分割の害が支配的
  (TuRBO 論文の多TR利得は大予算・超多峰で顕著)。機能自体は残置(大予算用)。

## 18. 2026-07-12: 評価回数70%削減ジョブ (/goal) — MFカスケードで達成(CFD系)

手順: 3並列文献調査(2021–2026、MF-BO / 高次元BO / サロゲート・獲得関数)→ 候補10件を
外部レビュー(sol)で吟味 → 上位を実装・A/B → 採否判定。詳細な候補リストと吟味結果は
本節の実測でクローズ。

### 18.1 MFカスケード `CascadeMFEngine`(python/trust_bo/multifidelity.py)— 採用

- 設計: LF(低忠実度)で全域探索(lf_budget)→ feasible上位から多様な top_k を選択 →
  そのバウンディングボックス(+10%マージン)に縮小した空間で HF エンジンを
  top_k 点のHF再評価からウォームスタート。`outside_frac` で HF 予算の一部を
  全域探索に確保可能(LF誤誘導防御、デフォルト0)。lf_config/hf_config 分離可。
- 検証: NeuralFoil CST 16D Cl/Cd、忠実度軸 = model_size(LF="xsmall", HF="xxxlarge")、
  8 seeds。ハーネス: `benchmarks/mf_ab_benchmark.py`、出典: `mf_ab_results.csv`。

| アーム | HF評価数 | mean | median | vs hf100 |
|---|---|---|---|---|
| hf100 (直接HF) | 100 | 161.1 | 162.7 | — |
| **casc30 (LF300+HF30)** | **30** | **265.5** | 267.1 | **GM 1.671、8/8勝** |
| casc50 | 50 | 273.3 | 272.1 | GM 1.721、8/8勝 |

- **判定: HF評価数70%削減を品質+67%で達成**(CFD系ユースケース)。
- 限界(正直に): LF/HF が同モデル系列で相関が非常に高い理想ペア。SU2 との実ペアでは
  相関低下で利得が縮む可能性 → 実SU2カスケード検証は未実施(ROADMAP MF-2)。
  外部監査(sol xhigh)の指摘を反映済み: top_k不足時のbackfill、lf_budget検証、
  step格子拒否、hf_feasible診断、lf/hf設定分離、outside_frac。

### 18.2 単一忠実度(合成)での70%削減 — 未達(正直な記録)

- 予算75 vs 基準(ei+early_frac=0.25, 予算250)、4関数×{50,100}D×ノイズ2×8 seeds。
  ハーネス: `benchmarks/goal70_ab_benchmark.py`。
- 結果: GM(base250/arm) は ts75=0.463 / ei75=0.467(達成セル 8/128, 0/128)。
  ackley/rastrigin は 0.85–0.87 まで迫るが、rosenbrock 0.10–0.11 が決定的。
- 判定: **忠実度軸のない問題では獲得関数・サンプリングの工夫だけで70%削減は不可能**。
  達成経路はMFカスケード(18.1)に限られる。

### 18.3 RAASP型次元マスク `cem_dim_mask` — 棄却

- 実装: グローバルCEMで各次元を確率 min(1,20/d) でのみ摂動(Papenmeier et al.
  ICML 2025 の知見のCEM適応)。初版は外部監査(sol xhigh)が「σの人工収縮
  (≈√p倍/反復)」欠陥を検出 → 次元別elite更新(摂動eliteのみでモーメント計算、
  不足時旧値保持、最低1次元摂動保証)に修正して再検証。
- 結果(修正版): raasp75 vs ei75(同予算) GM=**0.971**、raasp勝ち58/128 — 利得なし。
- 判定: **棄却**。TR が既に局所性を担保する本構成では次元マスクの出番がない
  (監査の予見どおり)。フラグはデフォルトoff・ビット一致確認済みで残置。

### 18.4 文献調査で選定したが未実施の候補(次回持ち越し)

bilog出力変換(SCBO/HEBO、小)、アンサンブルσ校正(中、要慎重A/B)、
入力拡張MF-MLP(LF予測をサロゲート特徴に注入、中、18.1の発展形)、
SU2実ペアでのカスケード検証。見送り確定: Gittins獲得関数、LogEI、グローバルGP、
REIリスタート、PFN系(CPU制約)、SU2早期打ち切り(goal外のwall-clock最適化)。

## 19. 2026-07-12: MF-2 相関ゲート — NeuralFoil→SU2 実ペアは不通過(カスケードA/B中止)

- 目的: §18.1 のカスケード(NeuralFoil同系列ペアで70%削減)が**実ペア**
  (LF=NeuralFoil xlarge / HF=SU2 RANS)でも成立するかの事前判定。
  文献基準(§18.4 R1調査): コスト比<0.2 かつ **相関 R²>0.75** が成立条件。
- 設定: CST 16D、96点(Sobol 64 + LF選抜32)、SU2 ITER=4000、
  新実装の幾何制約(§19.1)適用済み。ハーネス: `benchmarks/mf_su2_correlation.py`、
  出典: `mf_su2_correlation.csv`。Codex-onlyセッションで測定、Claude側で生CSVから独立再集計し一致確認。

| 指標 | 実測 | ゲート |
|---|---|---|
| feasibleペア | 57/96 | — |
| **Pearson R²** | **0.284** | >0.75 **不通過** |
| Spearman ρ | 0.689 | ≥0.75 不通過 |
| ρ bootstrap 2.5%下限 | 0.48–0.53 | — |
| feasibility一致率 | 0.66–0.69 | — |

- 判定: **ゲート不通過につきMF-2カスケードA/Bを中止**(見込み約17時間のSU2計算を回避)。
  NeuralFoil→SU2 は順位相関が中程度(ρ≈0.69)あるものの、値相関が弱く
  feasibility不一致も31%あり、カスケードの縮小box選定を誤誘導するリスクが大きい。
- 帰結: §18.1 の「70%削減」は **NeuralFoil同系列の高相関ペア限定**の結果として確定。
  実SU2最適化への適用は現構成では非推奨。代替経路は ROADMAP MF-2' 参照
  (LF=SU2粗メッシュ、NeuralFoil校正、相関が低くても残差学習で吸収するMF-3)。
- 教訓: **高コストA/Bの前に安価な相関ゲートを置く設計が機能した実例**
  (96点の測定でA/B全体を正しく中止できた)。

### 19.1 SU2 幾何 fail-fast 制約(Issue #3 対応、採用)

- `validate_cst_geometry`(benchmarks/su2/airfoil_mesh.py): 最大厚み比≥0.06、
  断面積≥0.05、上下面非交差をメッシュ生成前に判定。su2_runner.py は
  `cd≤1e-6` の非物理チェックも追加(§16.1 で ei が突いた CD≈0 アーティファクト対策)。
- テスト: tests/test_su2_geometry.py 8件(NACA0012合格・交差/薄翼/小断面の棄却・決定性)。
  §19 の測定は本制約適用下で実施され、Cl/Cd>300 の非物理値混入はゼロだった。

## 20. 2026-07-12: P2D / BILOG / MF-2' の検証(残ジョブ消化)

### 20.1 P2D(NeuralFoil): acquisition="ei" では phase2_early_frac は冗長

- 設定: §15.2 と同条件に ei_early(ei + phase2_early_frac=0.25)アームを追加。
- 結果: **全16ペアで ei と完全一致(GM=1.0000)**。切り分け実験(ackley 10D)で原因確定:
  ei では EI 停滞シグナルが反復15で Phase 2 を発火させ、TR 辺長が 0.25×l_init に
  縮む前に local 入りするため early_frac は一度も作動しない。ts 系では従来どおり有効
  (ts_early は別軌道、§16.2 の改善も ts 基準)。
- 判定: **early_frac は「停滞シグナルを持たない ts 系専用の補完機能」**。
  v0.3 デフォルト化(enable_phase2 時 0.25)は ei 利用者に副作用ゼロ・ts 利用者に
  利得となり、支持材料が増えた。SU2 側の確認は §20.3。

### 20.2 BILOG: bilog 出力変換 — 棄却

- 実装: `bilog_transform` フラグ(サロゲート学習値に sgn(v)ln(1+|v|)、TR best_value は
  生値のまま)。フラグ OFF ビット一致確認済み。
- 設定: base(ei+early_frac=0.25+enable_phase2)vs bilog、4関数 × {50,100}D × ノイズ{0,5%}
  × 8 seeds、budget 250。ハーネス: `benchmarks/bilog_ab_benchmark.py`。
- 結果: リグレット GM 比 base/bilog = **0.881**(bilog が12%悪化)、bilog 勝ち 53/128。
  本命の rosenbrock で **0.634(37%悪化)** — 外れ値減衰が谷の勾配情報まで圧縮し、
  サロゲートと Phase 2 残差 GP の分解能を落とす。ackley/rastrigin/levy も 0.97–1.00 で利得なし。
- 判定: **棄却**(SCBO/HEBO の報告は GP+制約付き文脈であり、MLP アンサンブル+
  z-score 構成には転移しない)。フラグはデフォルト off で残置。

### 20.3 P2D(SU2 実RANS): ei では early_frac 冗長を実RANSでも確定 → v0.3 デフォルト化判断

- 設定: §16.1 と同条件(budget 100、3 seeds)、新幾何制約下で ei / ei_early を取り直し
  (旧データとの混在を避け新CSV `su2_p2d_ab_results.csv`)。
- 結果: **ei_early は全3シードで ei と完全一致**(89.41 / 561.12 / 196.55)。
  ノイズ・infeasible 混在の実RANSでも EI 停滞シグナルが early_frac より先に
  Phase 2 を発火させる。なお ei の3シードは旧§16.1と同値 — 新幾何制約は
  この設計空間では発火率 ~0.1%(一様2000点で実測)のため軌道不変(正常)。
- **P2D 最終判定**: `phase2_early_frac=0.25` の v0.3 デフォルト化(enable_phase2 時)を
  **推奨**。根拠: ei 系には副作用ゼロ(NeuralFoil 16ペア+SU2 3シードで完全一致)、
  ts 系には明確な利得(§16.2 GM 1.372)。ビット挙動が変わるのは ts+enable_phase2
  利用者のみで、minor バージョン境界での変更として妥当。
- 副次観測: §16.1 の非物理値 561.12(seed1)は新制約下でも再現 — 幾何制約(厚み・面積)と
  cd≤1e-6 チェックの間を抜ける(cd が小さいが 1e-6 超)。対策として `min_cd`(デフォルト
  1e-3、Re=3e6 の乱流翼型で物理下限 ~4e-3 の余裕側)を SU2Settings に追加(§20.4)。

### 20.4 min_cd 非物理閾値(採用)

- `SU2Settings.min_cd = 1e-3`(configurable): CD ≤ min_cd を nonphysical として棄却。
  NACA0012 スモーク(CD≈0.014)には影響しない。§19 相関データで nonphysical 発火は
  cd≤1e-6 版で 10/96 — min_cd 強化により 561 級のすり抜けも遮断される見込み
  (次回の SU2 系ベンチから適用。P2D A/B は旧閾値で完結済みのため結果に影響なし)。

## 21. 2026-07-12/13: MF-2' 相関ゲート — SU2粗メッシュも不通過(カスケード再開見送り)

- 目的: §19(LF=NeuralFoil、R²=0.284で不通過)の後継として、同物理・低解像度の
  **SU2粗メッシュ**(LF: n_half=60, nj=45, max_iter=800)を LF とする相関を検証。
  同一ソルバー・同一物理モデルなら NeuralFoil より高相関になるという仮説。
- 設定: CST 16D、Sobol 48点(LF/HF とも同一点)。ハーネス:
  `benchmarks/mf_su2_coarse_correlation.py`、出典: `mf_su2_coarse_correlation.csv`。
  SMOKE(8点)で先行PASS(R²=0.859)を確認していたが、本走48点では逆転。

| 指標 | 実測(生CSVから独立再計算・一致確認済み) | ゲート |
|---|---|---|
| feasibleペア | 24/48 | — |
| **Pearson R²** | **0.447** | >0.75 **不通過** |
| Spearman ρ | 0.423 | ≥0.75 不通過 |
| ρ bootstrap 2.5%下限 | **0.013** | ほぼゼロ — 相関が標本依存で不安定 |
| feasibility一致率 | 0.833 | — |
| LF/HFコスト比 | 0.057 | <0.2 達成(コストは問題でない) |

- 判定: **不通過。MF-2カスケードの再開は見送り**(NeuralFoil・SU2粗メッシュの
  両LF候補が不合格)。SMOKE(n=8, R²=0.859)からの逆転は、少数点での見かけ上の
  高相関が本走で崩れた典型例 — **相関ゲートはSMOKE規模では判定を確定させず、
  本走(n≥30程度)まで待つべき**という運用上の教訓。
- 原因分析: 今回のLFはメッシュ解像度(n_half/nj)と反復数(max_iter)を同時に
  粗くしており、境界層剥離・遷移位置の予測がHFと構造的にずれた可能性が高い
  (2軸の忠実度低下を混在させた設計上の弱点)。
- **判定(確定)**: **NeuralFoil→SU2 系のマルチフィデリティ・カスケードは、
  現在検討した2つのLF定義(学習済みサロゲート/同物理粗解像度)のいずれでも
  本プロジェクトの設計空間・CST16Dパラメータ化では成立しない**。
  §18.1(70%削減)は同系列サロゲート内(NeuralFoil xsmall↔xxxlarge)限定の
  成果として確定させ、SU2を巻き込むカスケードは次善策(次段落)が固まるまで凍結する。

### 21.1 次善策(次回セッション以降、未着手)

- **反復数のみを落としたLF**(メッシュは HF と同一、max_iter のみ削減): 忠実度軸を
  1本に絞ることで交絡を除き、相関のクリーンな評価ができる可能性。ただし
  コスト比(LF/HF)が §19/§21 ほど小さくならない見込み(同メッシュなら
  1反復あたりのコストは同等)
- **境界層のみ粗くしない粗メッシュ**(first_cell/growth は HF 同一、遠方場のみ荒く):
  剥離・遷移予測を壊さずにセル数だけ削減する設計
- いずれも着手前に SMOKE(8点)ではなく直接20点規模で相関を仮測定し、
  「SMOKE規模の見かけ相関」に基づく判断を避けること(本節の教訓)

## 22. 2026-07-13: アンサンブルσのpost-hoc校正(CAL)— 棄却

- 実装: `sigma_calibration` フラグ(`src/surrogate.rs`)。主サロゲート学習時に
  データ末尾 min(n/5,20) 件をホールドアウト(n_dims+2 未満なら校正スキップ)、
  ホールドアウトは**強制 cold start**(warm_states 無視)で学習して真の
  out-of-sample 予測を確保 — 初版はwarm start経由でホールドアウトが既に学習済みの
  重みを継続使用しており漏洩していた欠陥をCodex sol監査が検出、修正済み。
  校正係数はNLL最小化の閉形式解 `c=sqrt(mean(((y-mu)/sigma)^2))` を **[1.0, 5.0]
  にクランプ(縮小方向は禁止)**。理由: 過去のコヒーレント単一メンバーTS(比1.375悪化)
  の教訓から、σを縮める変更は探索を阻害するリスクが高いため「過小評価の補正のみ」に
  用途を限定。非有限値は除外し、有効項が無ければ校正スキップ。
- 設定: 4関数 × {50,100}D × ノイズ{0,5%} × 8 seeds、budget 250、`acquisition="ts"`
  (σが `μ+z·σ` に直接効く経路)。ハーネス: `benchmarks/cal_ab_benchmark.py`、
  出典: `cal_ab_results.csv`。生CSVから独立再集計し一致確認済み。
- 結果: リグレット GM 比 base/cal = **1.008**(誤差範囲)、cal 勝ち 58/128。
  rosenbrock で最大の変化(GM 1.037)だが有意とは言えない水準。
- 判定: **棄却**。フラグはデフォルトoffで残置(ビット一致確認済み)。
  解釈: 縮小方向を禁止した保守的な校正では、そもそもこの5メンバー・アンサンブルの
  σが体系的に過小評価されていないか、TR局所領域では校正の余地が小さい可能性。
  クランプ下限を緩めて縮小方向も試す発展形は、コヒーレントTSの悪化実績を踏まえ
  優先度低(ROADMAP未記載のまま据え置き)。

## 23. 2026-07-14/15: 実CFDニッチ尖鋭化ジョブ(/goal 70-300%向上)— 候補6/1/9すべて棄却、候補11はゲート不通過

**背景**: 「エンジンの一番の得意分野(実CFD形状最適化, SU2 RANS, ei+phase2構成)をさらに
とがらせる」/goalジョブ。Codex sol 3並列調査(Phase2局所精密化/CEM効率化/制約・ノイズ・
並列効率)で11候補を抽出し、Codex sol選定で「候補6→1→9のコア実装+候補11(実行速度軸)」
というスコープを確定(詳細は候補調査ログ、当セッションのscratchpadに保存・非コミット)。
選定の時点でsol自身が「現実的な期待は評価数20-40%削減・品質10-30%改善、70%以上は
極めて野心的」と明言していた。

### 実装した3候補(いずれも`#[serde(default)]`でデフォルトfalse、フラグOFFはビット一致確認済み)

- **`cem_diverse_starts`**(候補6, 決定的多様化マルチスタート): CEM多スタート点をTR内
  top-3(目的値順)から、1位保持+残り2点を価値上位20件プール内の貪欲Farthest-Point選択に
  変更(`src/lib.rs`, `src/tr.rs::l2_dist`をpub(crate)化して再利用)。
- **`enable_mads_poll`**(候補1, Opportunistic MADS poll): Phase2(local)でincumbentが
  3ラウンド連続改善しないと、`poll_cycle`から決定的に選んだ2次元×±方向(最大4点)の
  coordinate poll候補をGP+EI候補と混ぜる。mesh初期値`l_frozen/4`、失敗ごとに半減
  (下限1e-4)、**連続発火5回で改善まで一時停止**(後述の監査で追加)。新規往復状態5個
  (`local_stagnation_count`, `poll_cycle`, `poll_mesh`, `local_best_value`,
  `poll_fire_count`)を`phase`/`stagnation_count`と同型のパターンでconfig/output/
  engine.pyのask・save・load全てに追加。
- **`joint_batch_select`**(候補9, 決定的joint batch選択): グローバル(単一TR・非ts_ei)
  バッチ選択を、固定半径0.1のgreedyから、アンサンブル5メンバーを決定的シナリオ集合とした
  joint marginal-improvement greedyに変更可能にした(`Ensemble::predict_members`新設、
  `batch::joint_greedy_select`新設)。**feasibility surrogateが存在する場合は
  既存greedyへ完全fallback**(後述の監査で確定、詳細は次節)。

### 監査で検出した実バグ(A/B投入前に2ラウンド修正、xhigh監査の価値を再実証)

sol xhigh監査1回目(申告によりネットワーク制限で外部sol呼び出しが失敗し、Codex内蔵の
代替監査2本で突合)で以下を検出:
- 候補9: feasibility加重running_best更新が**数式的に誤り**。反例
  (A: pred=1000 p_feas=0.1, B: pred=90 p_feas=1.0, C: pred=80 p_feas=1.0で
  期待値最大の{A,B}でなく{A,C}が選ばれる)を確認。座標重複除外の欠如も検出
  (旧greedy_selectの半径除外機能の退行)。
- 候補1: mesh下限(1e-4)到達後、同一poll点集合を無限に再評価しうる
  (評価効率という/goalの目的に直接反する)。

1回目の修正(閾値方式FEAS_THRESHOLD=0.5でlikely-feasible限定joint選択)は、
**xhigh監査2回目で「閾値以上でも同型バグが残存(根治していない)」と再指摘**
(p_feas=0.51の反例で再現)。候補1もmicro-GP fallback時に停止状態がリセットされる
経路を新規検出。最終的に**「feasibility surrogateが存在する場合は既存greedyへ
完全fallback」という監査明示の安全策**で確定(数式的に常に正しいが、SU2のような
制約付き問題ではfeasibility surrogate学習後は実質的にjoint選択が発火しなくなる)。
候補1はmicro-GP fallback時に候補1関連5状態を変更せず引き継ぐよう修正。
両修正後、cargo test 44件pass、regressionテスト3件(反例を直接再現)追加、
bitcheck全一致を独自に再検証。

**教訓**: 閾値ベースの部分修正では数式的な根本原因を消せないことがある
(RAASP σ収縮、CAL warm-startリークに続く3例目)。「制約ありでは安全な既存経路へ
完全fallback」という保守的設計が、複雑な部分修正より結果的に速く確実に正しさへ
到達した。sol xhigh監査は1回では不十分な場合があり、修正→再監査のループを
閉じるまで粘る価値がある。

### 候補11(BAPI型早期終了)Phase B1: オフラインROCゲート — 不通過

過去のSU2ベンチマークはworkdir削除(`shutil.rmtree`)により反復ごとの残差履歴が
残っていなかったため、新規にSU2実測200点(CST 16D一様乱数, seed=11001)でラベル付き
データセットを収集(feasible=77, bad_su2=69, pre_su2_reject=54)。iter=400/600/800の
早期残差特徴量(log10 RMS残差、直近区間の傾き、CD変化量等)で発散予測のROC分析を実施。

- 最高AUC 0.6915(iter=800, `rms_worst`閾値)。
- **FPR≤2%かつTPR≥30%を同時に満たす閾値は存在せず**(FPR≤2%での最大TPRは20.3%)。
- 判定: **ゲート不通過**。候補11のSU2 runner統合(Phase B2)は見送り確定。
- 懸念: bad_su2の69件中47件はSU2自体の発散でなくPhase B1データ収集用の300秒
  タイムアウト起因の可能性があり、判別性能を悪化させている可能性がある
  (独立収集でタイムアウトを分離すれば改善する余地はあるが、今回は採用しない)。
- `benchmarks/su2/su2_runner.py`に追加した`save_trajectory_path`引数(デフォルトNone
  で無効)は、他の棄却済みフラグと同じ扱いで残置。監査で検出した例外処理の不備
  (malformed dataがCFD結果を上書きする問題)は修正済み。

### SU2実測A/B本走: 候補6+9・候補6+9+1・候補1単体、いずれもbaseline未超え

baseline configは`acquisition="ei"+enable_phase2=True+phase2_early_frac=0.25+batch/
workers=8`(既存`su2_cfd_benchmark.py`はacquisition未指定でデフォルト"ts"になる不備が
あったため、新規ハーネス`benchmarks/goal2_su2_ab_benchmark.py`を作成)。
CST 16D、Ma=0.3 Re=3e6 SA、budget=100、**8 paired seeds**、10評価おきのbest-so-far
軌跡を記録するresumable設計(セッション中の環境再起動でも21/32完了分を保持して再開できた)。

4アーム(baseline / c6c9 / c6c9c1 / c1only)× 8 seeds = 32実行、全て完了。

| アーム | 最終品質 幾何平均比(対baseline) | 品質で勝ったseed数 | baseline最終品質への到達速度で勝ったseed数 |
|---|---|---|---|
| c6c9(候補6+9) | 0.806(19%悪化) | 3/8 | 0/8(4/8は期限内未到達) |
| c6c9c1(候補6+9+1) | 0.784(22%悪化) | 2/8 | 0/8(4/8は期限内未到達) |
| c1only(候補1単体) | 0.943(6%悪化、ほぼ横ばい) | 1/8 | 0/8(6/8はbaselineと完全一致) |

**24ペア比較(3アーム×8seed)を通じて、評価効率でbaselineに勝った例は1件もなし**
(evals_to_reach_baseline_finalが1.0未満のケースがゼロ)。

**解釈**:
- 候補9はfeasibility surrogate学習後は既存greedyへfallbackする安全設計のため
  ほぼ無寄与(c6c9とc6c9c1の悪化幅がほぼ同一であることが裏付け)。
- **候補6(決定的多様化マルチスタート)が主な悪化要因**と見られる。候補6を含む
  c6c9/c6c9c1は明確に悪化するが、含まないc1onlyはbaselineとほぼ同じ(6/8 seedで
  完全一致)。実CFDで探索の多様性を強める変更が裏目に出るのは、§16の
  ts(探索性重視)がSU2実RANSでei比0.353まで悪化した現象と同型のパターン。
- 候補1(MADS poll)は8シード中6シードで一度も有効なpollを発火/成功させず
  baselineと完全一致。「Phase2局所停滞への保険」という設計意図どおりには機能したが、
  実際に停滞を打開できるケースが少なかった。

### 総合判定: /goal未達、候補6/1/9はいずれもデフォルト非採用

70%floor(最低)・300%(目標)のいずれにも遠く届かず、そもそも**候補群単体では
v0.3.0 baselineを上回れない**という明確な負の結果。採否基準(5 seeds中4勝以上、
主指標の改善が明確)を満たすアームはゼロ。3フラグとも**デフォルトoffで残置**
(棄却済みフラグとして、bilog_transform等と同じ扱い)。

事前にsolが「70%以上は極めて野心的」と評価していた見立ては正しかった。
このエンジンは既に実CFDニッチで強い局所最適に近い状態にあり、追加の
アルゴリズム的介入(特に探索性を強める方向)は総じて逆効果になりやすいことが
本ジョブで改めて実証された(§17 dual TR、§18.3 RAASP、§20.2 bilog、§22 CALに続く
5件目・6件目・7件目の棄却)。

## 24. 2026-07-16: 3Dフロントウィング最適化パイプライン(実用検証)— 成功、初の3D CFD実行

**背景**: エンジン本来の目的(個人のFormula Studentカー空力最適化)を初めて3Dで実用検証する
ジョブ。既存の2D CST翼型パイプラインは完全に機能していたが、3D関連のコードはリポジトリに
一切存在しなかった(gmsh/FFD/STL/CAD等の痕跡なし、完全新規構築)。ユーザー判断により
「エンジンの本領発揮域(50-100D)を正面から検証する」方針を採用し、19D案(ルート=チップ
一定断面)から65D案(スパン制御点あり)へ計画段階で方針転換した。

### 実装(いずれも新規、`benchmarks/su2/wing3d_mesh.py`, `benchmarks/su2/wing3d_runner.py`,
`benchmarks/wing3d_benchmark.py`)

- **メッシュ生成**: gmsh不使用(libGLU/sudo制約、既存2D O-メッシュと同じ理由で再確認)。
  6スパン制御点(η=0,.2,.4,.6,.8,1.0固定)× 各10D(camber CST係数5+thickness CST係数5、
  camber/thickness分解でthickness係数を正値制約し自己交差を構造的に防止)+
  プランフォーム4D(半スパン・ルートコード・テーパー比・LE後退量)+ AoA 1D = **65D**。
  スパン方向はclamped cubic B-splineで補間。2D O-メッシュのリングをスパン方向に積み重ねる
  構造格子方式(押し出し)。半裁モデル(symmetry面)、平板エンドプレート、45,000セル弱
  (RAM 7.6GB環境向けに大幅に粗い)
- **SU2設定**: 文献調査で判明したFSAE実走行条件(V≈40km/h、Re≈1.5-3×10⁵)は既存2D
  ベンチのRe=3e6と1桁違うため、**`SOLVER=INC_RANS`(非圧縮)を新規採用**
  (圧縮性RANSの低Mach極限での数値散逸・収束悪化を回避)。地面はmovingウォール
  (`SURFACE_MOVEMENT=MOVING_WALL`)、半裁は`MARKER_SYM`
- **収束判定**: 接地効果流はCauchy収束せず振動する(周期約2800反復)ことを実機で発見。
  windowed-average(直近2500反復平均、変動係数・前後半差で安定性判定)とSU2自身の
  真のCauchy収束(優先)を組み合わせた判定ロジックを新規実装
- **並列スケーリング実測**(20コア・RAM7.6GB環境): 1並列×20スレッド≈300秒/評価、
  **2並列×10スレッド≈444秒/評価(実効222秒/評価、最良)**、4並列×5スレッドは
  タイムアウトするほど非効率。WORKERS=2を採用

### 監査(sol xhigh、3ラウンド要した)

1回目: NO-GO。CONFIRMED4件、最重要は**Claude自身が実装した`su2_cauchy`判定**が
文字列マッチのみで、SIGTERM+exit code 0で23反復の過渡状態を収束扱いにする実バグ
(監査が実SU2へのSIGTERM注入で反例を実証)。他: Cauchy時平均範囲がSU2自身の基準と
最大12.5%ずれ、JSONL torn write回復不能、resume時に評価条件変更を検出しない。
2回目: 一部RESOLVEDだが、JSONL中間行破損の黙殺とfingerprint3欠落(SU2バージョン取得が
実際には壊れている・WORKERS除外が誤り・メッシュ物理設定が対象外)が残存し依然NO-GO。
3回目: 上記全てRESOLVED確認、新たにfingerprintの実装ファイル網羅不足(軽微、Claude自身が
直接修正)とエンドプレート壁面解像度の粗さ(y+数百〜数千のオーダー、既知の限界として
ユーザー承認の上で受容)を検出してGO。

### 本走結果(budget=250, WORKERS=2, ITER=6000, single seed)

全250評価完了、**feasible=209/250(83.6%)**。

| 評価数 | best objective(ダウンフォース/ドラッグ比) |
|---|---|
| n_init=50(コールドスタート終了時) | 12.62 |
| 100評価 | 14.05 |
| 150評価〜250評価 | **14.63**(プラトー) |

**コールドスタート比+16%改善**。最良設計: CL=-0.828(ダウンフォース方向、符号正しい)、
CD=0.0566、AoA=2.72°(範囲[-5°,15°]の中間付近、5つのプランフォーム/AoAパラメータ
いずれも境界に非依存)。infeasible内訳: `unexpected_nonnegative_cl`(揚力方向)28件、
`geometry_section_area_ratio`10件、`not_converged`3件、いずれも正しい理由での除外。

**物理的妥当性の検証**: feasible 209件でAoA-CL相関**-0.75**、AoA-CD相関**+0.988**
(ほぼ完全な正相関)、|CL|-CD相関**+0.66**——教科書通りの空力挙動(迎角依存性、
lift-induced drag)が確認された。最良解群のAoAはサンプル最大値(13.34°)でなく
2.3-3.2°に集中しており、「ダウンフォース最大化」でなく「効率(比)最大化」という
目的関数の意図通り、最大揚力角でなく効率の谷間を正しく探索できている。

### 評価と限界

定性的な流体力学挙動は物理的に一貫しており、CFDとして機能していることは高い確度で
言える。一方、メッシュが文献規模(260万〜660万セル)の1/50〜1/150程度に粗く、
エンドプレート解像度の限界、低Re域での完全乱流SA仮定という制約から、**絶対値
(CL/CD/効率比の具体的な数字)を実車設計に直接使うには追加のメッシュ細分化・検証が
必要**。「候補を大量から少数に絞り込む一次スクリーニング」としては実用的、
「最終設計の根拠」としてはまだ実用的でない、という位置づけ。

**教訓**: 新規メッシュ・ソルバー設定・収束判定という3つの完全新規実装が重なると、
監査1回では収束せず3ラウンド要した。特に「監査で検出されたバグの修正に、
修正した本人(Claude)が気づかなかった新たなバグが監査で発覚する」という
パターンが2回続けて起きた(1回目→2回目の遷移)。新規実装への信頼は
「動いた」ではなく「独立監査を通過した」で担保すべきという、既存の教訓
(§候補9の閾値方式根治せず)をさらに補強する事例。
